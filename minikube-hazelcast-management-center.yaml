---
# =========================================================
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pod-hazelcast-management-center
spec:
  replicas: 1
  serviceName: bankinabox-hazelcast-management-center-service
  selector:
    matchLabels:
      app: pod-hazelcast-management-center
  template:
    metadata:
      labels:
        app: pod-hazelcast-management-center
    spec:
      containers:
        - env:
          - name: JAVA_OPTS
            value: "-Dhazelcast.mc.allowMultipleLogin=true -Dhazelcast.mc.healthCheck.enable=true"
          image: "hazelcast/management-center:latest"
          imagePullPolicy: IfNotPresent
#          livenessProbe:
#            httpGet:
#              path: /hazelcast-mancenter/health
#              port: 8081
          name: container-hazelcast-management-center
#          readinessProbe:
#            httpGet:
#              path: /hazelcast-mancenter/health
#              port: 8081
#            initialDelaySeconds: 30
---
# =========================================================
---
apiVersion: v1
kind: Service
metadata:
  name: bankinabox-hazelcast-management-center-service
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: pod-hazelcast-management-center
  type: LoadBalancer
---
